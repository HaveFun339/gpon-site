# Local development example (do NOT commit real secrets)
# Vite client env (only used for optional mailto fallback)
VITE_CONTACT_EMAILS=epicgamesfun4@gmail.com,yuventos1979@gmail.com

# Optional client-side EmailJS (will be embedded into the bundle)
# If you use CDN/init method you only need the public key and service/template ids
VITE_EMAILJS_SERVICE_ID=service_o70i822
VITE_EMAILJS_TEMPLATE_ID=template_xxx
VITE_EMAILJS_PUBLIC_KEY=y7coKI9qlUdywXJ_Y

# Server (do NOT prefix with VITE_ — these stay server-side)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
EMAIL_FROM=no-reply@example.com
EMAIL_TO=
